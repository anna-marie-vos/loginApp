const React = require('react')
const debug = require('debug')('components:profile')

module.exports = function (props) {
  debug({props})
  
  return (
    <div>
      profile renders
    </div>
  )
}
